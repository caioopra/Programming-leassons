const people = ['yoshi', 'mario', 'peach', 'bowser'];
const ages = [10, 20, 30, 40]

// console.log(people);

module.exports = {
    people,  // same as doing people: peole, where the first one is the name that is used to access
    ages
}